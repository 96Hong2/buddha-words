# prettier 가 손대지 않을 것.
#
# 생성물과 CSS 둘뿐이다. 나머지는 전부 prettier 를 통과한다.

# ── 생성물. 저장소에 남지 않는다 ────────────────────────────────────────────
dist/
playwright-report/
test-results/
e2e/shots/

# ── CSS 는 prettier 를 통과시키지 않는다 ────────────────────────────────────
# 값 hex 를 대문자로 적기로 정해 두었는데(docs/plan/05-비주얼-디자인-언어.md) prettier 는
# 이걸 소문자로 내린다. 토큰 파일 하나에서만 42개가 바뀐다. 주석을 칸 맞춰 세운 램프 표도
# 같이 흐트러진다. 색 표기 규칙이 포매터보다 먼저다.
*.css
