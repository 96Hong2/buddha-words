/**
 * 위기·위로·실패 화면의 고정 문구와 창구 정본.
 *
 * 이 파일의 문장은 모델이 만들지 않는다. 무슨 말이 나올지 심사에 제출할 수 없는 자리를
 * 생성에 맡기지 않는다. 자해의 방법이나 상황은 한 글자도 쓰지 않고 연락할 곳만 적는다.
 */

import type { Channel } from '../../shared/api';

export const CRISIS_COPY = {
  title: '지금 많이 힘드신 것 같아요',
  lead: '이 이야기는 지금 바로 사람과 나누는 것이 좋겠어요. 24시간 들어 줄 곳이 있어요.',
  urgent: '지금 위험한 상황이라면 112 또는 119',
  continueNote:
    '이야기를 계속 들어 드릴 수도 있어요. 다만 해결책을 드리기보다 곁에 있는 쪽에 가까울 거예요.',
  continueCta: '그래도 이야기를 들어주세요',
  draftKept: '적으신 글은 지워지지 않았어요. 닫으면 그대로 남아 있어요',
  close: '닫고 돌아가기',
} as const;

export const SOLACE_COPY = {
  /**
   * 경전 출처 뒤에 붙는다.
   *
   * 전에는 ' · 현대적 풀이'였는데 방향이 반대였다. 이 화면에서 인용부 안에 있는 글은
   * 경전 원문이고, 풀어 쓴 말은 그 위아래의 opening·closing 이다.
   * 원문에 「현대적 풀이」를 붙이면 경전 문장이 AI 가 옮긴 말처럼 읽힌다.
   *
   * 감수를 말하지 않는 이유: 위로 화면에 오르는 구절도 초안일 수 있다. 개발·dev 후보 풀은
   * 399구절 전부라 감수 전 구절이 그대로 올라온다. 여기 적는 문장은 그 구절이 무엇인지
   * 가르는 말이지 감수 도장이 아니다. 감수 여부는 구절마다 다르므로 고정 문구로 적지 않는다.
   */
  citeSuffix: ' · 경전 원문',
  micro: [
    // 이 화면에서 AI 가 쓴 것은 위로하는 말이다. 경전은 아니다.
    //
    // 「사용 횟수를 세지 않고 광고도 나오지 않아요」를 뺐다. 힘들다고 적은 사람에게
    // 우리 쪽 셈법(횟수·광고)을 설명할 자리가 아니다. 그 사람에게 필요한 말은
    // 이 글을 누가 썼고 이것이 무엇을 대신하지 못하는가뿐이다.
    '위로하는 말은 AI 가 썼어요 · 전문가의 도움을 대신하지 않아요',
  ],
  rewrite: '다른 이야기 쓰기',
} as const;

export const ERROR_COPY = {
  title: '지금은 답을 만들지 못했어요',
  /** 어떤 실패인지 알 수 없을 때의 기본 안내 */
  lead: '잠시 뒤에 다시 해볼까요?',
  /* 「남은 횟수」라고 적지 않는다. 하루 천장을 없애 셀 것이 없다(2026-09-20) */
  quotaKept: '다시 보내도 오늘 쓴 횟수는 늘지 않아요',
  retry: '다시 해보기',
  close: '닫기',
  offlineBanner: '인터넷이 끊겼어요. 연결되면 이어서 할 수 있어요',
  offlineKept: '쓰던 이야기는 그대로 있어요. 연결되면 그 자리에서 보낼 수 있어요.',
  partialTitle: '나머지 풀이를 못 불러왔어요',
  partialBody: '위에 있는 세 가지는 그대로 볼 수 있어요. 다시 받아도 오늘 횟수는 늘지 않아요.',
  partialRetry: '나머지 다시 받기',
} as const;

/*
  ⚠ **마들랜에는 누를 것을 두지 않는다.** 이 앱에서 여는 데 세 번 실패했다(2026-09-25 실기기):
  운영 기관 홈페이지 · 공식 카카오톡 채널 · `sms:109` 가 모두 눌러도 아무 일이 없었다.
  같은 화면의 `tel:` 과 `mailto:` 는 열린다.

  네 번째 주소를 찾는 대신 링크를 걷었다. **위기 화면에서 눌러도 안 열리는 버튼은 막다른
  길이고, 그것이 안내가 아예 없는 것보다 나쁘다.** 누를 것이 없으면 헛걸음도 없다.

  창구 자체는 남긴다. 말로 하기 어려운 사람에게 글로 잇는 자리가 여기 하나뿐이다.
  대신 카드가 **찾아가는 방법**을 알려 준다. 이 길은 링크와 무관하게 통한다.
*/

export interface ChannelInfo {
  /** 창구 카드 첫 줄 */
  name: string;
  /** 창구 카드 둘째 줄. 번호 또는 연결 수단 */
  value: string;
  /** 창구 카드 셋째 줄 */
  note?: string;
  /** 위로 답변의 한 줄 띠에서 보이는 이름 */
  short: string;
  /**
   * 누르면 여는 주소. **없으면 누를 것이 없는 안내 카드다.**
   *
   * 이 앱은 웹 주소를 걸지 않는다(웹뷰가 못 연다). 기기 앱이 받는 스킴만 건다.
   * 어느 쪽도 안 되는 창구는 주소를 비우고 `value` 가 찾아가는 방법을 말한다.
   */
  href?: string;
  kind: 'call' | 'sns';
}

/**
 * 번호와 이름은 복지부 안내를 따른다. 109 가 위기 1순위이고 마들랜이 글로 잇는 자리다.
 * 어느 창구를 어떤 순서로 보일지는 서버가 정한다. 화면은 받은 순서대로 그린다.
 */
export const CHANNELS: Record<Channel, ChannelInfo> = {
  '109': {
    name: '자살예방상담전화',
    value: '109',
    note: '24시간 · 걸면 상담원과 바로 이야기해요',
    short: '자살예방상담전화 109',
    href: 'tel:109',
    kind: 'call',
  },
  madeleine: {
    name: '자살예방 SNS 상담 마들랜',
    value: '카카오톡에서 「마들랜」 검색',
    note: '말로 하기 어려우면 글로 이야기할 수 있어요. 109로 문자를 보내도 돼요',
    short: '마들랜',
    kind: 'sns',
  },
  '1388': {
    name: '청소년전화',
    value: '1388',
    short: '청소년전화 1388',
    href: 'tel:1388',
    kind: 'call',
  },
  '1366': {
    name: '여성긴급전화',
    value: '1366',
    short: '여성긴급전화 1366',
    href: 'tel:1366',
    kind: 'call',
  },
  '112': { name: '경찰', value: '112', short: '경찰 112', href: 'tel:112', kind: 'call' },
  '1577-0199': {
    name: '정신건강상담전화',
    value: '1577-0199',
    short: '정신건강상담전화 1577-0199',
    href: 'tel:1577-0199',
    kind: 'call',
  },
};

const DEFAULT_CHANNELS: Channel[] = ['109', 'madeleine'];

/** 창구는 조건부로 감추지 않는다. 서버가 빈 목록을 주더라도 기본 두 곳은 보인다 */
export function resolveChannels(channels: Channel[] | undefined): Channel[] {
  if (channels == null || channels.length === 0) return DEFAULT_CHANNELS;
  return channels;
}
